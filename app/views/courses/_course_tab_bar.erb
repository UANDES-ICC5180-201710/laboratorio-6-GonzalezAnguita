<div class="row">
    <div class="col-lg-4 col-lg-offset-4">
        <ul class="nav nav-pills" role="tablist">
            <% if clicked_tab == 1%>
                <li role="presentation" class="active"><%= link_to @course.title, course_path(@course) %></li>
            <% else %>
                <li role="presentation"><%= link_to @course.title, course_path(@course) %></li>
            <% end %>

            <% if clicked_tab == 2 %>
                <li role="presentation" class="active"><%= link_to 'Assignments', course_assignments_path(@course) %></li>
            <% else %>
                <li role="presentation"><%= link_to 'Assignments', course_assignments_path(@course) %></li>
            <% end %>

            <% if clicked_tab == 3 %>
                <li role="presentation" class="active"><%= link_to 'Students', students_course_path(@course) %></li>
            <% else %>
                <li role="presentation"><%= link_to 'Students', students_course_path(@course) %></li>
            <% end %>
        </ul>
    </div>
</div>

<br>