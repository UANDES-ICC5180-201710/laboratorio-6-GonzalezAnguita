<p id="notice"><%= notice %></p>

<%= render 'courses/course_tab_bar', clicked_tab: 3 %>

<%= render 'people/list', people: @course.students %>

<br>

<%= link_to 'New Student', new_course_enrollment_path(course_id: @course.id) %>
