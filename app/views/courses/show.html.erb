<p id="notice"><%= notice %></p>

<%= render 'courses/course_tab_bar', clicked_tab: 1 %>

<p>
  <strong>Code:</strong>
  <%= @course.code %>
</p>

<p>
  <strong>Teacher:</strong>
  <%= @course.teacher %>
</p>

<%= link_to 'Edit', edit_course_path(@course) %> |
<%= link_to 'Back', courses_path %>
